#include <iostream>
#include <fstream>
#include <vector>
#include <algorithm>

using namespace std;

vector<int> primeFactors(int n);

int power(int n, int r, int q);

int lucasTest(int n);

void print_lucas(vector<int> v, string name);